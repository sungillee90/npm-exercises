// math.js USING "IMPORT"
const add = (x, y) => x + y;
const subtract = (x, y) => y - x;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;

export default {add, subtract, multiply, divide}; // es6 shorthand

// module.exports = keys;